import React, { Component } from 'react';
import { Media, Button } from 'reactstrap';
import { NavLink } from 'react-router-dom';

class BearEndpoint extends Component {
  render() {
    return(
      <div className="container">
      </div>
    );
  }
}
export default BearEndpoint;
